---
name: pr-review-simulation
description: >-
  外部メンテナーが直接採用・取り込みすることを想定した公開コードbranchを、
  push、更新、Pull Request提出の前にメンテナーの立場でレビューする。PR本文は任意。
  branch全体の取り込みを想定しないintegration、測定、診断、棄却実験は対象外。
  Use before publishing or submitting a concrete code branch that an external
  maintainer may directly adopt or integrate. A PR body is optional.
  Do not use for non-mergeable integration, measurement, diagnostic, or rejected
  experimental branches. Triggers: 公開candidateのメンテナーレビュー、
  upstream候補branchのpush前レビュー、直接取り込める状態かの確認、
  PR候補コードの提出前レビュー、PR草案とコードのレビュー。
---

# 提出先を模した公開candidateレビュー

自分がupstreamオーナーではないリポジトリへ公開コードbranchをpush・更新・提出する前に、
**提出先のメンテナーを模したレビュアー**にコードと証拠を読ませ、
そのbranchを直接取り込めるかを試す。PR本文がある場合は本文も対象にするが、
PR本文の有無は適用条件ではない。

対象は、外部メンテナーが直接採用・取り込みすることを想定した変更である。
自分のリポジトリ内のレビューは `design-review` を使う。
実際にcandidateをレビューするときは**この2つを併用する。** 適用対象かどうかだけを
判定する場合は、`design-review`まで実行しなくてよい。`design-review`は変更そのものの設計を見る。
このスキルは、その変更が**提出先の文脈で判断できる形になっているか**を見る。

## 適用判定

次をすべて満たす場合に使う。

1. 対象が外部メンテナーの管理するリポジトリ（自分がフォークしたものを含む）への変更である。
2. 具体的なbase commitとコード差分がある。
3. 公開branchのコードを、メンテナーが直接採用・取り込みすることを想定している。
4. 公開branchのpush・更新またはPull Request提出より前である。

Pull RequestやPR本文はなくてよい。公開branchがPRなしで直接取り込まれる可能性も対象に含む。

### 対象外

- branch全体の取り込みを想定しない総合検証用integration branch
- 測定専用branch、診断branch、棄却済み実験の保存branch
- 通常の技術文書レビュー
- 自分自身がupstreamオーナーであるリポジトリ内のレビュー

対象外だと分かった場合は、PR相当のレビューへ勝手に拡張しない。対象外である理由を述べ、
個別candidateの指定、`japanese-technical-writing`、`doc-discipline`、`design-review`など、
実際の目的に合うレビューへ切り替える。

## なぜ自分では見つからないか

**知識の初期化は、自分の中ではできない。**
書き手は調査の全文脈を持っており、忘れたふりをしても、通じない箇所は見えないままである。

公開candidateで問題になるのは、たいてい次の3つである。どれも自分では気づけない。

- 書き手側の事情を書いている。ブランチ運用、測定機材、自分の中だけで通用する番号付け
- 測定や解析の結論が、提出先が確かめられない形になっている
- candidateの説明が、実際の提出先のコードと食い違っている

3つ目は最も重い。**自分の理解が間違っていることを、自分では検出できない。**

## 手順

### 1. レビュー資料を汚染なしで作る

**`git archive` で `.git` を含まない素のツリーを展開する。**

    mkdir -p bundle/their-repo
    git -C <repo> archive <candidateのbase commit> | tar -x -C bundle/their-repo

worktreeやcheckoutをそのまま渡してはいけない。オブジェクトストアを共有するため、
`git log --all` で自分の作業branchやcommitメッセージが見える。前提が汚染される。

**今回の変更を判断するために必要なコードを参照できるようにする。**
対象リポジトリに加え、変更の影響を受ける利用側や、契約の確認に必要な依存先を含める。
所有者が同じという理由だけで無関係なコードを含めず、所有者が違っても判断に必要なら含める。

参照できることと、全文を読むことは分ける。変更箇所と、呼び出し・依存・契約を確認するための
関連箇所から読み、判断に必要な疑問が残る場合に範囲を広げる。

生成物、画像、フォント、`node_modules` は削る。

資料一式はこうなる。

    their-repo/              提出先のソース（candidateのbase）
    their-app/               影響や契約の確認に必要な利用側。必要な場合だけ含める
    candidate.diff           candidateの差分。生成物は除く
    candidate-diffstat.txt   生成物を含む変更ファイルと行数
    candidate-commits.txt    base以後のcommit順、件名、変更範囲
    patches/                 commit単位を評価する必要がある場合のpatch series
    evidence/                test、測定、provenanceなど、取り込み判断に必要な証拠
    PR-BODY.md               PR本文。存在する場合だけ含める

**混入チェックをかける。** 自分の調査リポジトリ名、独自のbranch名、独自の番号付けが資料に入っていないかをgrepで確認する。

### 2. ペルソナを最小限にする

**「あなたはこのコードを熟知しています」という説明を書かない。**  
書くのは、立場と、今回の変更に関係する担当範囲だけでよい。

    あなたは <repo> のメンテナーです。
    <app> もあなたが作っています。<module> はあなたが書いたコードです。
    外部から公開candidate branchが提示されました。直接取り込めるかを判断してください。

関連する担当範囲がない場合は、`<app>` や `<module>` の説明は省く。

PR本文がある場合だけ、「外部からPull Requestが届きました」として本文も資料に含める。

**知らないことを明示する。** 投稿者が誰か、どんな調査をしたか、どんな機材で測ったか、
投稿者独自の用語と番号付けは知らない。

### 3. コードを読めることを条件に織り込む

これを書かないと、レビューの半分が「コードを見れば分かること」の指摘で埋まる。

    コード、commit構成、証拠を突き合わせたうえで答えてください。
    PR本文がある場合は、それもコードと証拠に突き合わせてください。
    コードを読めば分かることを「説明に書いていない」という指摘にしないでください。
    逆に、コードを読んでも解決しなかった疑問は重要な指摘です。

### 4. 質問を決める

    1. このbranchを、明示されたbase、依存関係、commit順を保って取り込めると判断できますか
    2. 提出先の既存設計と保守方針を尊重していますか
    3. 必要以上の抽象、依存、CI、公開API、保守対象を増やしていませんか
    4. commit単位と依存順は、そのまま取り込める形ですか
    5. 効果と正しさの主張は、このcandidate自身の証拠で支えられていますか
    6. 未確認範囲は取り込みを止める問題ですか。別問題として分離できますか
    7. コードを読んでも解消しなかった疑問はどこですか
    8. 公開APIの意味が変わるなら、利用側にどんな追随作業が発生しますか

PR本文がある場合だけ、次も質問する。

    9. 本文の記述とコードの実際が食い違っている箇所はありますか
    10. 本文の数値や主張のうち、根拠が支える範囲を超えているものはありますか
    11. 取り込み判断に使わない情報はどれですか

**「良かった点の列挙は不要です」と書く。** 判断に使わない。

メンテナーが既に選んでいる設計、テスト配置、CIの有無、抽象化の程度を出発点にする。
正しさや今回の目的に必要だとコードと証拠から示せない限り、新しいCI、helper、抽象、
防御的fallbackを要求しない。保守対象を増やす提案には、具体的な必要性と既存設計では
満たせない理由を求める。

### 5. 疑いがあるなら、結論を伏せて質問にする

自分で既に疑っている箇所があるなら、末尾に別の節を立て、**質問の形で置く。**

    書き手で既に疑いが出ている箇所です。結論は伏せます。ご自分で確かめて、正否を述べてください。
    - 証拠の表の各行は、それぞれどの値を置けば再現できますか
    - candidateは「Xになる」と説明しています。実際にその状態は作られますか

**結論を書くと追認するだけのレビューになる。**
独立に同じ結論へ至れば確度が上がり、違う結論なら自分の誤りを疑える。

## 結果の扱い

**レビュー結果は主張であって判定ではない。重いものは自分でコードに当たって確かめる。**  
シミュレーションは間違うことがあり、条件の与え方次第で偽の問題も作る。

指摘は3つに分ける。

- **コードで確認できた事実** — 直す
- **こちらの説明が実態と違うという指摘** — 証跡に当たって、どちらが正しいかを決める
- **資料の与え方が作った問題** — 次の回で設定を直す。指摘としては捨てる

3つ目の例。提出先が所有する利用側アプリを渡さなかった回では、公開APIの意味変更について
「利用側にどう影響するか書かれていない」と指摘された。渡した回では、レビュアーが自分で
利用箇所を読んで「表示だけで判定には使っていないので追随不要」と結論した。
**指摘が消えたのではなく、資料が足りていなかった。**

## 繰り返す

直したら、**前の版を知らない新しいレビュアー**で回す。
「これは改訂版です」と伝えると、前回の指摘が閉じたかに注意が向き、
candidateを直接取り込めるかという本来の問いが薄まる。

回を重ねると指摘の質が上がる。表面的な説明の指摘から、設計と事実の指摘へ移っていく。
**設計や事実の指摘が出るようになったら、candidateを判断できる資料になった印である。**

## `design-review` との併用

順序は問わないが、役割を混ぜない。

    design-review            変更そのものの設計。抽象、fallback、依存、保守性
    pr-review-simulation     提出先の文脈で判断できるか。説明と事実の一致

**両方が同じ指摘を返すことがある。** その指摘は深刻である。

## やらないこと

- 提出先の実在の人物の判断を予測したと述べない。**模したレビュアーの出力である**
- レビュー結果をそのままPR本文へ引用しない
- 指摘の数を成果にしない。**candidateを直接取り込めると判断できる状態になったかだけが目的である**
- `upstream`、`README`、`メンテナー`、`レビュー`という単語だけでこのスキルを適用しない
- 取り込み対象でないbranchや文書へ、個別PRのmerge判断に必要な情報を要求しない
